# ──────────────────────────────────────────────────────────────
# ECHOS Experiment Dependencies
# Python 3.10+  |  CUDA 12.x  |  2× L40 (96 GB total VRAM)
# ──────────────────────────────────────────────────────────────

# Core ML
torch>=2.3.0
torchvision>=0.18.0

# HuggingFace stack
transformers>=4.45.0
datasets>=2.21.0
accelerate>=0.33.0
peft>=0.12.0

# Quantization (for fp4 / int8 modes)
bitsandbytes>=0.43.3

# Scientific stack
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0          # mutual information, optional

# LoRA / merging math
einops>=0.8.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0

# Optional (for Figure 3 network graphs)
networkx>=3.3

# Optional (for symbolic math equivalence in MATH eval)
sympy>=1.13.0

# SWE-bench harness (install separately if needed)
# swebench>=2.1.0

# Utilities
tqdm>=4.66.0
rich>=13.7.0
jsonlines>=4.0.0
